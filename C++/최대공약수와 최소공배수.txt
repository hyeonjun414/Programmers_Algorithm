#include <string>
#include <vector>

using namespace std;

int gcd(int n, int m)
{
	while (m)
	{
		int r = n % m;
		n = m;
		m = r;
	}
	return n;
}
vector<int> solution(int n, int m) {
	vector<int> answer;
	answer.push_back(gcd(n, m));
	answer.push_back(n*m/gcd(n, m));
	return answer;
}
